## TBOX车辆管理系统Git提交

### 2024.05.13-2024.05.17

> #### 2024/05/13

```
Adjust: 车辆位置坐标转换。

1. 解决vue项目调用百度地图API跨域的问题。
2. 对车辆位置的经纬度进行坐标转换。
3. 保存最新的坐标，下一次接收时做对比，一致则不用转换，不一致则转换坐标。

Adjust: Vehicle position coordinate conversion.

1. Solve the cross-domain problem of the Vue project calling Baidu Map API.
2. Perform coordinate conversion on the longitude and latitude of the vehicle location.
3. Save the latest coordinates and compare them the next time you receive them. If they are consistent, there is no need to convert them. If they are inconsistent, the coordinates will be converted.
```

> #### 2024/05/17

```
Modify: 参数配置版本修改。

硬件版本修改为SOC版本，固件版本修改为MCU版本。

Modify: Parameter configuration version modification.

The hardware version is modified to the SOC version, and the firmware version is modified to the MCU version.
```

```
Modify: 参数配置版本修改。

硬件版本修改为MCU版本，固件版本修改为SOC版本。

Modify: Parameter configuration version modification.

The hardware version is modified to the MCU version, and the firmware version is modified to the SOC version.
```

```
Add: 执行标准添加。

侧边栏底部添加“执行标准: GB/T32960.3-2016”字样。

Add: Perform standard addition.

Add the words "Executive Standard: GB/T32960.3-2016" at the bottom of the sidebar.
```

```
Modify: 历史数据样式修改。

删除实时查询中的根据类型筛选，调整按钮底部bottom间隔。

Modify: Modify historical data style.

Delete filtering by type in real-time query and adjust the bottom interval at the bottom of the button.
```

